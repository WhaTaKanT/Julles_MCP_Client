<Window x:Class="MCP_DevSolution_1_FrontendClient_ModelContextProtocol.ProfileManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCP_DevSolution_1_FrontendClient_ModelContextProtocol"
        mc:Ignorable="d"
        Title="Manage Connection Profile" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Profile Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ProfileNameTextBox" Margin="5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Server Hostname/IP:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="ServerHostTextBox" Margin="5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Server Port:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="ServerPortTextBox" Margin="5"/>

        <!-- Placeholder for more options if needed -->

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveProfileButton" Content="Save" Width="75" Margin="5" Click="SaveProfileButton_Click"/>
            <Button x:Name="CancelProfileDialogButton" Content="Cancel" Width="75" Margin="5" Click="CancelProfileDialogButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
