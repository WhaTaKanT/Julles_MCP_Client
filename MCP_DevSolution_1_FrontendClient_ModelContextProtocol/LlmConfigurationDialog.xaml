<Window x:Class="MCP_DevSolution_1_FrontendClient_ModelContextProtocol.LlmConfigurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCP_DevSolution_1_FrontendClient_ModelContextProtocol"
        mc:Ignorable="d"
        Title="Manage LLM Configuration" Height="auto" Width="500" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Config Name -->
            <RowDefinition Height="Auto"/> <!-- Provider Type -->
            <RowDefinition Height="Auto"/> <!-- API Endpoint -->
            <RowDefinition Height="Auto"/> <!-- API Key -->
            <RowDefinition Height="Auto"/> <!-- Model Name -->
            <RowDefinition Height="Auto"/> <!-- System Prompt -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Configuration Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ConfigNameTextBox" Margin="5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Provider Type:" VerticalAlignment="Center"/>
        <!-- ProviderType could be a ComboBox if we have a predefined list, for now TextBox -->
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="ProviderTypeTextBox" Margin="5" ToolTip="Examples: OpenAI-Compatible, LMStudio, OpenAI, Claude, Gemini"/>

        <Label Grid.Row="2" Grid.Column="0" Content="API Endpoint:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="ApiEndpointTextBox" Margin="5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="API Key (Optional):" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="ApiKeyTextBox" Margin="5"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Model Name (Optional):" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="ModelNameTextBox" Margin="5" ToolTip="Specific model ID if endpoint serves multiple, e.g., gpt-4, llama-2-7b-chat.Q5_K_M.gguf"/>

        <Label Grid.Row="5" Grid.Column="0" Content="System Prompt (Optional):" VerticalAlignment="Top" Margin="0,5,0,0"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="SystemPromptTextBox" Margin="5" TextWrapping="Wrap" MinLines="3" MaxHeight="150" VerticalScrollBarVisibility="Auto" AcceptsReturn="True"/>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="SaveButton" Content="Save" Width="75" Margin="5" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="75" Margin="5" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
